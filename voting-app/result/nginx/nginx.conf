events { worker_connections 1024; }

stream {
    server {
        listen 6379;
        proxy_pass redis-server:6379;
    }

    server {
        listen 6380;
        proxy_pass redis-server:6379;
    }
}
